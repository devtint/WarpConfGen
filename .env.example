# Example environment variables for WarpGen
# Set WEBHOOK_URL to enable server-side notifications when a key is generated.
# Leave WEBHOOK_URL empty or unset to disable webhook delivery.

# Optional webhook URL (POST JSON):
WEBHOOK_URL=

# Optional explicit webhook read URL (JSON API).
# If empty, the app will try to derive a read endpoint from WEBHOOK_URL for webhook.site.
WEBHOOK_READ_URL=

# Track webhook read results up to this date (inclusive), format YYYY-MM-DD.
WEBHOOK_CUTOFF_DATE=2026-02-25

# Server-side stats file path (optional):
STATS_FILE=warpgen_stats.json

# Vercel / production: set WEBHOOK_URL in your deployment environment variables
